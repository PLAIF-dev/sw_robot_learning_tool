{
  "name": "robot-learning-tool",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"npm run dev:be\" \"npm run dev:fe\"",
    "dev:be": "dotnet run --project backend/RobotLearningTool.Api",
    "dev:fe": "npm --prefix frontend install && npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
